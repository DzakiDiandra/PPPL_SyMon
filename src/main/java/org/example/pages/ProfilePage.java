package org.example.pages;

import org.example.SymonBasePage;
import org.openqa.selenium.WebDriver;

public class ProfilePage extends SymonBasePage {

    public ProfilePage(WebDriver driver) {
        super(driver);
    }
}
